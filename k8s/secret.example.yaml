# TEMPLATE ONLY — placeholders, safe to commit.
#
# Copy this to k8s/secret.yaml (gitignored), replace every value with a real
# runtime secret, and apply it. NEVER commit the filled-in secret.
#
#   cp k8s/secret.example.yaml k8s/secret.yaml
#   # edit k8s/secret.yaml with real values
#   kubectl apply -f k8s/secret.yaml
#
# These are the SERVER-ONLY secrets the standalone Next.js server reads at
# runtime. They are never passed at build time and never inlined into the bundle.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: pwyw
type: Opaque
stringData:
  DATABASE_URL: "postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true"
  DIRECT_URL: "postgresql://USER:PASSWORD@HOST:5432/postgres"
  SUPABASE_SERVICE_ROLE_KEY: "replace-me"
  STRIPE_SECRET_KEY: "sk_test_replace-me"
  STRIPE_WEBHOOK_SECRET: "whsec_replace-me"
